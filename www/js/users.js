var app = angular.module('cityzen.users', []);

app.service('UserService', function (store) {

    var service = {
        getUser: function(userId) {
            
        }
    };
    return service;
});